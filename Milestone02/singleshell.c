#include <stdio.h>

int main(){
	char *command;
	fprintf(stdout, "Execute: ");
	/* Get command for code to run */
	
	/* Check input to see if command can be ran */

	/* Execute command */
	fprintf(stdout, "Executing: %s\n", command);
	char *result;

	/* Output results */
	fprintf(stdout, "%s\n", results);

	fprintf(stdout, "Execution complete!\n");
	return 0;
}
